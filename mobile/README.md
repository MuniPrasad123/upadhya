# Mobile

The Flutter learner application is intentionally deferred. Its eventual API integration should use generated contracts, accessible components, offline-aware behavior, and privacy-preserving local storage.
